# 1. Ver el estado actual (qué cambió)
git status

# 2. Agregar los cambios al "staging area"
git add .

# 3. Hacer un commit con mensaje claro
git commit -m "Descripción corta de lo que cambiaste"

# 4. Subir a GitHub
git push origin main
